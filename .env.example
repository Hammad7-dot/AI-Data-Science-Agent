# Phase 0 MVP does not need any API keys - Planner and Coder are
# deterministic, rule-based stand-ins for an LLM call (see TODOs in
# app/planner.py and app/coder.py). Uncomment and fill in below once a
# real LLM integration is wired up in a later phase.

# OPENAI_API_KEY=

